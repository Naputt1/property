/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../services/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  PostAdminResetMutationResponse,
  PostAdminReset500,
} from "../models/PostAdminReset.ts";

function getPostAdminResetUrl() {
  const res = { method: "POST", url: `/admin/reset` as const };
  return res;
}

/**
 * @description Truncate properties and jobs tables, clear queues, and clear analytics cache
 * @summary Reset backend state
 * {@link /admin/reset}
 */
export async function postAdminReset(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PostAdminResetMutationResponse,
    ResponseErrorConfig<PostAdminReset500>,
    unknown
  >({
    method: "POST",
    url: getPostAdminResetUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
