/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../services/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type { PostAuthLogoutMutationResponse } from "../models/PostAuthLogout.ts";

function getPostAuthLogoutUrl() {
  const res = { method: "POST", url: `/auth/logout` as const };
  return res;
}

/**
 * @description Log out the current user
 * @summary User logout
 * {@link /auth/logout}
 */
export async function postAuthLogout(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PostAuthLogoutMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getPostAuthLogoutUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
