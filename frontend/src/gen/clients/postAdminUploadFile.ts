/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../services/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  PostAdminUploadFileMutationRequest,
  PostAdminUploadFileMutationResponse,
  PostAdminUploadFile400,
  PostAdminUploadFile500,
} from "../models/PostAdminUploadFile.ts";
import { buildFormData } from "../.kubb/config.ts";

function getPostAdminUploadFileUrl() {
  const res = { method: "POST", url: `/admin/upload-file` as const };
  return res;
}

/**
 * @description Upload a CSV file via multipart/form-data to bucket and queue a migration job
 * @summary Upload CSV file directly
 * {@link /admin/upload-file}
 */
export async function postAdminUploadFile(
  data: PostAdminUploadFileMutationRequest,
  config: Partial<RequestConfig<PostAdminUploadFileMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = data;
  const formData = buildFormData(requestData);
  const res = await request<
    PostAdminUploadFileMutationResponse,
    ResponseErrorConfig<PostAdminUploadFile400 | PostAdminUploadFile500>,
    PostAdminUploadFileMutationRequest
  >({
    method: "POST",
    url: getPostAdminUploadFileUrl().url.toString(),
    data: formData as any,
    ...requestConfig,
  });
  return res.data;
}
