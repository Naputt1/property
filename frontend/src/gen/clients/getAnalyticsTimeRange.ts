/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../services/kubb-client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  GetAnalyticsTimeRangeQueryResponse,
  GetAnalyticsTimeRange401,
  GetAnalyticsTimeRange500,
} from "../models/GetAnalyticsTimeRange.ts";

function getGetAnalyticsTimeRangeUrl() {
  const res = { method: "GET", url: `/analytics/time-range` as const };
  return res;
}

/**
 * @description Get minimum and maximum year available in the dataset
 * @summary Get available time range for analytics
 * {@link /analytics/time-range}
 */
export async function getAnalyticsTimeRange(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetAnalyticsTimeRangeQueryResponse,
    ResponseErrorConfig<GetAnalyticsTimeRange401 | GetAnalyticsTimeRange500>,
    unknown
  >({
    method: "GET",
    url: getGetAnalyticsTimeRangeUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
