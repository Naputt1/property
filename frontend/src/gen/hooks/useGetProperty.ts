/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  GetPropertyQueryResponse,
  GetPropertyQueryParams,
  GetProperty500,
} from "../models/GetProperty.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getProperty } from "../clients/getProperty.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getPropertyQueryKey = (params: GetPropertyQueryParams = {}) =>
  [{ url: "/property" }, ...(params ? [params] : [])] as const;

export type GetPropertyQueryKey = ReturnType<typeof getPropertyQueryKey>;

export function getPropertyQueryOptions(
  params?: GetPropertyQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPropertyQueryKey(params);
  return queryOptions<
    GetPropertyQueryResponse,
    ResponseErrorConfig<GetProperty500>,
    GetPropertyQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getProperty(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Get a list of UK housing properties with pagination and filtering
 * @summary List properties
 * {@link /property}
 */
export function useGetProperty<
  TData = GetPropertyQueryResponse,
  TQueryData = GetPropertyQueryResponse,
  TQueryKey extends QueryKey = GetPropertyQueryKey,
>(
  params?: GetPropertyQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPropertyQueryResponse,
        ResponseErrorConfig<GetProperty500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getPropertyQueryKey(params);

  const query = useQuery(
    {
      ...getPropertyQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetProperty500>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
