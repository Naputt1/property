/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  GetAdminJobsQueryResponse,
  GetAdminJobsQueryParams,
} from "../models/GetAdminJobs.ts";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getAdminJobs } from "../clients/getAdminJobs.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getAdminJobsSuspenseQueryKey = (
  params: GetAdminJobsQueryParams = {},
) => [{ url: "/admin/jobs" }, ...(params ? [params] : [])] as const;

export type GetAdminJobsSuspenseQueryKey = ReturnType<
  typeof getAdminJobsSuspenseQueryKey
>;

export function getAdminJobsSuspenseQueryOptions(
  params?: GetAdminJobsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getAdminJobsSuspenseQueryKey(params);
  return queryOptions<
    GetAdminJobsQueryResponse,
    ResponseErrorConfig<Error>,
    GetAdminJobsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getAdminJobs(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Get a list of background jobs with pagination
 * @summary List background jobs
 * {@link /admin/jobs}
 */
export function useGetAdminJobsSuspense<
  TData = GetAdminJobsQueryResponse,
  TQueryKey extends QueryKey = GetAdminJobsSuspenseQueryKey,
>(
  params?: GetAdminJobsQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetAdminJobsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getAdminJobsSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getAdminJobsSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
