/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  GetAdminJobsQueryResponse,
  GetAdminJobsQueryParams,
} from "../models/GetAdminJobs.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getAdminJobs } from "../clients/getAdminJobs.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getAdminJobsQueryKey = (params: GetAdminJobsQueryParams = {}) =>
  [{ url: "/admin/jobs" }, ...(params ? [params] : [])] as const;

export type GetAdminJobsQueryKey = ReturnType<typeof getAdminJobsQueryKey>;

export function getAdminJobsQueryOptions(
  params?: GetAdminJobsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getAdminJobsQueryKey(params);
  return queryOptions<
    GetAdminJobsQueryResponse,
    ResponseErrorConfig<Error>,
    GetAdminJobsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getAdminJobs(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Get a list of background jobs with pagination
 * @summary List background jobs
 * {@link /admin/jobs}
 */
export function useGetAdminJobs<
  TData = GetAdminJobsQueryResponse,
  TQueryData = GetAdminJobsQueryResponse,
  TQueryKey extends QueryKey = GetAdminJobsQueryKey,
>(
  params?: GetAdminJobsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetAdminJobsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getAdminJobsQueryKey(params);

  const query = useQuery(
    {
      ...getAdminJobsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
