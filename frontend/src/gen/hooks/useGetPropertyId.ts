/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  GetPropertyIdQueryResponse,
  GetPropertyIdPathParams,
  GetPropertyId404,
} from "../models/GetPropertyId.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getPropertyId } from "../clients/getPropertyId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getPropertyIdQueryKey = (id: GetPropertyIdPathParams["id"]) =>
  [{ url: "/property/:id", params: { id: id } }] as const;

export type GetPropertyIdQueryKey = ReturnType<typeof getPropertyIdQueryKey>;

export function getPropertyIdQueryOptions(
  id: GetPropertyIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPropertyIdQueryKey(id);
  return queryOptions<
    GetPropertyIdQueryResponse,
    ResponseErrorConfig<GetPropertyId404>,
    GetPropertyIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getPropertyId(id, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Get detailed information for a specific property
 * @summary Get property by ID
 * {@link /property/:id}
 */
export function useGetPropertyId<
  TData = GetPropertyIdQueryResponse,
  TQueryData = GetPropertyIdQueryResponse,
  TQueryKey extends QueryKey = GetPropertyIdQueryKey,
>(
  id: GetPropertyIdPathParams["id"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPropertyIdQueryResponse,
        ResponseErrorConfig<GetPropertyId404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getPropertyIdQueryKey(id);

  const query = useQuery(
    {
      ...getPropertyIdQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetPropertyId404>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
