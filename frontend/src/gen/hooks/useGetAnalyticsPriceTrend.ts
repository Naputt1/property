/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../services/kubb-client";
import type {
  GetAnalyticsPriceTrendQueryResponse,
  GetAnalyticsPriceTrendQueryParams,
  GetAnalyticsPriceTrend500,
} from "../models/GetAnalyticsPriceTrend.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getAnalyticsPriceTrend } from "../clients/getAnalyticsPriceTrend.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getAnalyticsPriceTrendQueryKey = (
  params: GetAnalyticsPriceTrendQueryParams = {},
) => [{ url: "/analytics/price-trend" }, ...(params ? [params] : [])] as const;

export type GetAnalyticsPriceTrendQueryKey = ReturnType<
  typeof getAnalyticsPriceTrendQueryKey
>;

export function getAnalyticsPriceTrendQueryOptions(
  params?: GetAnalyticsPriceTrendQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getAnalyticsPriceTrendQueryKey(params);
  return queryOptions<
    GetAnalyticsPriceTrendQueryResponse,
    ResponseErrorConfig<GetAnalyticsPriceTrend500>,
    GetAnalyticsPriceTrendQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getAnalyticsPriceTrend(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Get average and median price trends over time
 * @summary Get price trend analysis
 * {@link /analytics/price-trend}
 */
export function useGetAnalyticsPriceTrend<
  TData = GetAnalyticsPriceTrendQueryResponse,
  TQueryData = GetAnalyticsPriceTrendQueryResponse,
  TQueryKey extends QueryKey = GetAnalyticsPriceTrendQueryKey,
>(
  params?: GetAnalyticsPriceTrendQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetAnalyticsPriceTrendQueryResponse,
        ResponseErrorConfig<GetAnalyticsPriceTrend500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getAnalyticsPriceTrendQueryKey(params);

  const query = useQuery(
    {
      ...getAnalyticsPriceTrendQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetAnalyticsPriceTrend500>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
